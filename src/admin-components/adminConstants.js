/** Finance policy surfaced in admin UI (must match settlement logic). */
export const PLATFORM_COMMISSION_RATE = 0.15;
export const RESTAURANT_NET_RATE = 0.85;
export const RIDER_PAYOUT_PER_COMPLETED_DELIVERY_INR = 40;
